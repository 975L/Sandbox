/* javascript for sandbox.975l.com */

//Defines current filePath
var filePath = $('script[src*=functions]').attr('src');
filePath = filePath.replace('functions.min.js', '');

$(document).ready(function() {
});